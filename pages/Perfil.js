import { View, StyleSheet, Text } from 'react-native';

const Perfil = () => {
    return (
        <View>
            <Text>
                Datos del perfil
            </Text>
        </View>
    );
};

const styles = StyleSheet.create({
    container:{
        flex:1,
    }
})

export default Perfil;